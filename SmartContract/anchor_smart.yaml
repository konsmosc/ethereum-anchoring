apiVersion: v1

kind: Pod

metadata:
  name: anchorsmart
  labels:
    app: anchorsmart

spec:
  restartPolicy: "Never"
  containers:
    - name: anchorsmart-container
      image: mabdockerid/anchor_smart:latest
      imagePullPolicy: Always
